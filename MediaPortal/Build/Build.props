<?xml version="1.0" encoding="utf-8"?>
<Project ToolsVersion="4.0" xmlns="http://schemas.microsoft.com/developer/msbuild/2003">
  <!--
  This file defines commonly used MSBuild properties.
  It is a common include file, that can imported by calling MSBuild project files.
  -->

  <PropertyGroup>

    <ProjectRoot Condition="$(ProjectRoot) == ''">$(MSBuildProjectDirectory)\..</ProjectRoot>
    <NuGetExePath Condition="$(NuGetExePath) == ''">$(MSBuildProjectDirectory)\NuGet.exe</NuGetExePath>
    <MSBuildCommunityTasksPath Condition="$(MSBuildCommunityTasksPath) == ''">$(ProjectRoot)\Packages\MSBuildTasks.1.4.0.61\tools</MSBuildCommunityTasksPath>

  </PropertyGroup>
</Project>